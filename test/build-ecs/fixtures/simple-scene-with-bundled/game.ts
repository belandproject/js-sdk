import { toJsonRpcRequest } from 'eth-connect'
import { test } from 'dcl-test-lib-integration'
import { aaa } from 'rollup-demo-beland-lib'

log(toJsonRpcRequest === test())
aaa()
engine.addEntity(new Entity())
